import { Module } from '@nestjs/common';

@Module({
  // Will contain CSV ingestion and processing features
  // controllers: [IngestionController],
  // providers: [IngestionService],
})
export class IngestionModule {} 